import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#14b8a6",
        },
        
        background: {
            default: "#0f172a",
            paper: "#1e293b",
        },
    },
});